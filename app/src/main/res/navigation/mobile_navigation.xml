<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/nav_home"> <!-- Set your desired start destination -->

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/nav_home"
        android:name="com.sr.trackcrack.ui.home.HomeFragment"
        tools:layout="@layout/fragment_home" />

    <!-- History Fragment -->
    <fragment
        android:id="@+id/nav_history"
        android:name="com.sr.trackcrack.ui.history.HistoryFragment"
        android:label="@string/menu_history"
        tools:layout="@layout/fragment_history" />

    <!-- Analytics Fragment -->
    <fragment
        android:id="@+id/nav_analytics"
        android:name="com.sr.trackcrack.ui.analytics.AnalyticsFragment"
        android:label="@string/menu_analytics"
        tools:layout="@layout/fragment_analytics" />

    <!-- Map Fragment -->
    <fragment
        android:id="@+id/mapFragment"
        android:name="com.sr.trackcrack.ui.map.MapFragment"
        tools:layout="@layout/fragment_map" />
</navigation>