<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/button_color" /> <!-- Button background color -->
    <corners android:radius="16dp" /> <!-- Soft corners with radius -->
    <padding
        android:bottom="8dp"
        android:left="16dp"
        android:right="16dp"
        android:top="8dp" />
    <stroke
        android:color="@color/button_border_color"
        android:width="1dp" /> <!-- Optional border -->
</shape>
